<!DOCTYPE html>
<html lang=”en”>
    <head>
        
    </head>
  
    <body>
        <h1> <%= title %> </h1>
        <ul>
            <% for(var i=0;i<albums.length;i++){ %>
                <li><h3> <%= albums[i].title %> </h3> <br>
                <%= albums[i].description %> <br>
                <%= albums[i].imageUrl %><br>
                <%= albums[i].createdDate %> <br>
                <%= albums[i].lastViewedOn %></li >
            <% } %>
        </ul> 
    </body>
</html>